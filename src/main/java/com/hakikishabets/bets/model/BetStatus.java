package com.hakikishabets.bets.model;

public enum BetStatus {
    PENDING,
    WON,
    LOST,
    CANCELLED
}

// Enum (short for "enumeration") — a special Java type
// that defines a fixed set of constants.
// In this case, it represents all possible statuses a bet can have.